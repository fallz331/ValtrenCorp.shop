import './globals.css';

export const metadata = {
  title: 'Valtren Corp',
  description: 'Digital ecosystem by Valtren Corp',
};

export default function RootLayout({ children }) {
  return (
    <html lang="id">
      <body>{children}</body>
    </html>
  );
}n
